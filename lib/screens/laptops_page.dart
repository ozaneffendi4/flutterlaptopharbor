import 'package:flutter/material.dart';

class LaptopsPage extends StatelessWidget {
  const LaptopsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Laptops Page", style: TextStyle(fontSize: 20)),
    );
  }
}
